/*
    --DEFINICION CON EL OBJETO DE JAVASCRIPT QUE SE LLAMA ARRAY

    const equipos = new Array("DeporCali", "Nacional", "America");

    --DEFINIENDO EL ARREGLO DE FORMA LITERAL ::: Recomendada
*/

const equipos = ["DeporCali", 197, true, "string", 100, false, true];
const paLlenar = [];
const preDefinido = [, , , ,];

// Llenado de arreglos

paLlenar[3] = 1000;

console.log(`La longitud del arreglo es: ${paLlenar.length}`);  // Literal expresion
// console.log("La longitud del arreglo es: " + paLlenar.length);  // Clasica con concatenador
